{
  "name": "ai-adaptive-practice-project",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm run build --workspace client",
    "start": "npm start --workspace server",
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace client\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}